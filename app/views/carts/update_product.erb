function updateQuantity(new_quantity, products_order_id) {
const targetProductQuantityText = document.getElementById(`products-order-quantity-${products_order_id}`);
targetProductQuantityText.innerHTML = `Quantity: ${new_quantity}`;
}
updateQuantity(<%= @target_products_order.quantity %>, <%= @target_products_order.id %>);